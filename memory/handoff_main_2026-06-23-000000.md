# Handoff - main - 2026-06-23 00:00

RESUME HERE: Author + run an FMEA (failure-mode enumeration) workflow across the ENTIRE kl-agents build, to find completeness gaps BEFORE they become patches. Greg approved whole-build scope and said it needs a DEEP-RESEARCH arm (import failure taxonomies from outside our own code - unknown-unknowns). I was mid-AskUserQuestion on 4 scoping decisions when Greg hit context-full; he did NOT answer them - re-ask (condensed) or proceed on the defaults below. The proven mechanism to adapt is the CKL FMEA workflow at `/Users/gregfoster/.claude/projects/-Users-gregfoster-00-coding/9b3f49e7-ef42-40eb-bf41-4d7357bd9950/workflows/scripts/ckl-completeness-fmea-wf_2c0ae388-fea.js` (read it - it is the template: ground-truth readers -> 6 adversarial lenses -> completeness critic -> synthesize, with a FINDING_SCHEMA). The new run is TWO-SOURCE: (1) that adversarial enumeration grounded in our as-built + (2) a deep-research arm importing wild failure taxonomies, then each researched failure checked against our build for an owner/UNGUARDED.

session-ended: 2026-06-23 00:00
trigger: typed
memory-review: none-this-session (all lessons this session already live in user-level memory files I READ, not new - see Decisions; nothing new to route except possibly the agent-FMEA taxonomy itself, which is the deliverable not a lesson)
relational-integration: n/a-no-vault-resident-artifact (code-repo session; kl-agents files are not in the K&L vault relational library)

## Goal
Two arcs this session: (1) DONE - built Bucket 3 (light agent platform contract), all 6 slices, merged to main. (2) OPEN - Greg pivoted to a SYSTEMIC concern: stop building agents component-by-component and discovering failures by running into them (the "patch quilt"). Wants an upstream, systematic, COMPOUNDING failure-anticipation process applied across the whole agents build. Next action is the FMEA run.

## Touched (this session, kl-agents repo - ALL COMMITTED + PUSHED, main @ 508761a)
- `shared/config.py` (NEW, 3a): StorageConfig moved here (live field name `supabase_service_role_key`, NOT the plan's `service_role_key`). Cut the shared->agents seam.
- `shared/db/client.py` (3a): imports StorageConfig from shared.config now.
- `agents/scout/config.py` (3a): StorageConfig def deleted; re-exports from shared.config (`__all__=["StorageConfig"]`) so existing refs work.
- `shared/db/migrations/0008_agent_runs_metrics.sql` (NEW, 3b): metrics jsonb + relax 4 posts_* counts to nullable + backfill + agent-neutral status comment. NOTE: numbered 0008 (next sequential), NOT the plan's placeholder "0010". APPLIED LIVE + verified on agents_dev (20 rows backfilled, 0 mismatch) and agents_prod (empty) of project zbokrrcexjecrkpogjqv.
- `shared/db/migrations/README.md` (3b): added 0008 row.
- `shared/runs.py` (NEW, 3c): RunHandle, start_run, finish_run(status, metrics: dict[str,MetricValue], error_summary, legacy_counts), finish_safely, status_to_exit_code, KLAgentConfig Protocol, _validate_metrics. MetricValue = str|int|float|bool|None (flat scalars).
- `agents/scout/storage/runs.py` (DELETED, 3c): all 5 importers repointed to shared.runs (main, posts, smoke_storage, test_storage, test_orchestrator).
- `agents/scout/main.py` (3c+3d): imports from shared.runs + shared.logging_setup; _status_to_exit_code removed (uses shared); _finish_safely replaced by _scout_counts + _finish_scout_run (writes metrics + mirrors legacy_counts during transition); all configure() calls pass agent="scout".
- `agents/scout/storage/posts.py` (3c): RunHandle import repointed to shared.runs.
- `scripts/smoke_storage.py`, `scripts/smoke_youtube_fetch.py`, `scripts/smoke_reddit_fetch.py` (3c+3d): repointed imports, finish_run new signature, configure(agent="scout").
- `shared/logging_setup.py` (NEW, 3d): JsonFormatter(agent) ctor injects agent DIRECTLY (no record.agent lookup, no "scout" fallback). configure(level, *, agent) required.
- `agents/scout/logging_setup.py` (DELETED, 3d).
- `docs/agent-contract.md` (NEW, 3e): the 5 contract requirements + the 1 hard invariant (shared/ never imports agents/). KLAgentConfig has 3 members (agent_name, snapshot, storage); agent_version carried IN snapshot, not a direct read.
- `tests/shared/__init__.py`, `tests/shared/test_runs.py`, `tests/shared/test_import_invariant.py`, `tests/shared/test_contract_surface.py` (NEW, 3c+3f): AST import-invariant test (proven to fail on injected violation), KLAgentConfig Protocol check (__protocol_attrs__), run-lifecycle tests (rejection path proven to fail without guard).
- `tests/scout/test_storage.py`, `tests/scout/test_orchestrator.py` (3c+3d): lifecycle tests moved out to tests/shared/test_runs.py; finished_with assertions now read metrics; fake patches finish_safely.
- `PLAN.md`, `PLAN-REVIEW-LOG.md`, `SPEC.md` (earlier this session): Bucket 3 Codex round 1+2+3+4 (APPROVED), Greg sign-off on Buckets 2+3.
- Final state: 94 tests pass, mypy clean, ruff clean, invariant holds, end-to-end storage smoke verified live on agents_dev (metrics + legacy mirror both written, confirmed by row inspection, then cleaned up). Branch feat/bucket-3-agent-contract merged --no-ff and deleted.
- UNTRACKED (deliberately NOT committed): `uv.lock` - byproduct of my `uv pip install`; project doesn't manage a lockfile. Leave it or gitignore it; do not commit without Greg's call.

## Open
- **THE resume point - FMEA run across the whole build.** 4 scoping decisions Greg did NOT answer (re-ask condensed OR proceed on these recommended defaults):
  1. Build scope: **Whole build, all seams** (Scout-live + Validation-spec'd-unbuilt + Echo-built + shared spine + cross-artifact seams SPEC/ROADMAP/migrations/live-DB + external-API + shared-backend). Greg explicitly said whole-build, not one area.
  2. Research depth: Greg explicitly said it needs deep research. Default **Deep, multi-domain**: failure taxonomies for multi-agent-on-shared-DB, Reddit/YouTube API-consumer agents, LLM-classifier drift/eval, cron/scheduled-agent ops, schema-migration-on-live-data. Each imported mode checked against our build for owner/UNGUARDED.
  3. Output: default **Map + seeded taxonomy + gate design** - (a) completeness map (gaps ranked, each adjudicated GUARD/ACCEPT/DEFER), (b) seed reusable agent failure-mode taxonomy body, (c) fail-closed enforcement design (a TEST/gate, not a process step - per captured-not-enforced). Report only, no code built until Greg reviews.
  4. Cadence: default **Staged with checkpoints** (ground-truth + research first, Greg reviews, THEN enumeration + synthesis) - matches how buckets were run, keeps him steering.
- **Enforcement principle (load-bearing, do not violate):** the deliverable that ENDS the patch quilt is a FAIL-CLOSED TEST/GATE (like the import-invariant test shipped in 3f), NOT a doc. Writing the taxonomy doc ≠ installing the system. The CKL spine report (`~/.claude/operator-os/ckl-spine-completeness-critique.md`) is the cautionary tale: 4 docs + 29 fixtures, 100% of runtime substrate UNBUILT = "frozen bucket-15." Don't repeat it.
- **The compounding piece:** every gap that ESCAPES the taxonomy becomes a new permanent category in the taxonomy body (same loop as memory). That ratchet is what makes this different from "a better checklist."
- **Roadmap is STALE (separate open thread, surfaced not fixed):** `.planning/ROADMAP.md` is the OLD provisional plan (Symptom Extension / Validation / Reddit API), predates ADR 0001's Scout re-aim, has NO Bucket 3 phase, and names a CONFLICTING `0008_symptom_classification.sql` (we shipped a different 0008). Greg asked me to "update roadmap status for bucket 3"; I surfaced that there's no bucket-3 row to flip + the conflicts, asked how to handle, he pivoted to the systemic FMEA topic instead. STILL OWED: reconcile/rebuild the roadmap (it's the grill's "rebuild roadmap from 3-bucket SPEC" task). The 0008 collision is itself a perfect FMEA example (cross-artifact seam, failed silently) - fold it in.
- **Grill arc remaining (pre-existing):** Echo (light pass) then rebuild provisional GSD roadmap from the 3-bucket SPEC. Bucket 2 (Validation) is Codex-APPROVED + Greg-signed but UNBUILT.
- **Deferred in-code (tracked):** drop deprecated posts_* columns + remove the transition legacy-mirror-write - a LATER migration, after Echo + dashboards stop reading them (in PLAN.md Out-of-scope).

## Decisions (do NOT re-litigate)
- **Systemic problem named (Greg, 2026-06-23):** component-by-component builds + reactive serial failure-discovery = patch quilt. The patches are the symptom; the disease is building by addressing failures we HIT rather than the failure SURFACE we enumerated. Want failure-discovery moved UPSTREAM (before build), made SYSTEMATIC (taxonomy not brainstorm), COMPOUNDING (grows from every escape), and ENFORCED (fail-closed, not advisory).
- **The 3 proven moves (from image-verify + CKL FMEA, all already battle-tested in Greg's other builds):** (1) RUNTIME-DERIVE not freeze - verifier walks a body file at runtime, runs every check incl. ones added after it was written (image Stage 0b; kills the frozen-checklist disease that lost I5/I6/O/K for months). (2) NON-OMISSIVE SCHEMA - completeness as a fixed set of categories forcing you into negative space (CKL report sections: SOLVED-with-falsifiers / Defect-classes / PROBABLY-INCOMPLETE / KNOWN-UNKNOWNS / UNTESTED-ASSUMPTIONS / WHAT-WAS-NOT-LOOKED-AT) + 6 parallel adversarial lenses (seams / topology-wrong / thin-guards / how-would-we-know / what-guards-the-guards / accepted-vs-unexamined) + a completeness critic. (3) DECIDE EVERY GAP - GUARD/ACCEPT-with-reason/DEFER-with-trigger; zero gaps in the UNEXAMINED-default state (a named-accepted gap is safe, a silent one is the disease).
- **Codex/grills are imagination-bounded + frozen** - they ask "does X exist Y/N", not "is X whole / are there other failure modes / is the CLASS covered". That's the ceiling the runtime-derived taxonomy + deep research break.
- **Key source files READ this session (don't re-derive, re-read):** `~/.claude/projects/-Users-gregfoster-00-coding/memory/feedback_enumerate_failure_surface_before_building.md` (the principle, dated 2026-06-23, has the 5 enumeration questions + the accountability line "completeness is competent design not a feature to request"); `~/.claude/commands/kl/image-verify.md` (Stage 0b runtime-derive is the anti-freeze core); the CKL FMEA workflow script (the mechanism); `~/.claude/operator-os/ckl-spine-completeness-critique.md` (the non-omissive schema + the frozen-bucket-15 cautionary verdict); `~/.claude/memory/captured-not-enforced-gap.md` (loading ≠ enforcing; the fix is always a hook/gate that BLOCKS, never a new capture doc).
- **Bucket 3 mechanical decisions (already shipped, for context):** migration is 0008 not 0010; StorageConfig field is supabase_service_role_key; KLAgentConfig = 3 members (agent_version lives in snapshot); finish_safely moved to shared (generic), _scout_counts stays in main (Scout-specific); legacy mirror-write is the transition mechanism (finding #5).
- **Supabase access:** the `supabase-pat` MCP is READ-ONLY (can't apply migrations). The `claude_ai_Supabase` MCP CAN write - used it with project_id `zbokrrcexjecrkpogjqv` (kl-agents; the OTHER project `frbcsyclhlljweqwjbqc` kl-operator-platform is INACTIVE, ignore). Test env: `.venv` needed `uv pip install` of supabase/google-api-python-client/etc to get a green baseline; editable install maps agents+shared to THIS dir correctly (a traceback path mentioning /Users/gregfoster/projects/kl-agents is stale bytecode, that dir doesn't exist).
- **Push-to-main needs explicit Greg auth** - the auto classifier blocked a direct push to main on vague "whatever you recommend"; Greg then explicitly authorized. Remember for next push.
