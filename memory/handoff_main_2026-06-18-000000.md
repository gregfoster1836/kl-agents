# Handoff — main — 2026-06-18 00:00

RESUME HERE: Bucket 2 (Validation Agent) spec is mid-Codex-review. Round 1 returned 11 findings; I applied 10 fixes to SPEC.md/SPEC-EVAL.md but have NOT yet re-submitted to Codex for round 2. The 11th finding is an OPEN SCOPE QUESTION awaiting Greg: Codex proposed Validation v1 be simpler (a read-only "top themes + candidate verbatims" REPORT + a tiny ledger, deferring LLM probe-drafting) vs. the full agent as specced. Greg did NOT answer (he called context-window-full instead). That question must be re-asked before Bucket 2 finalizes. Codex thread for resume: `019ed947-0557-76b1-8790-3a78cdda104f` (read-only resume; use `-c sandbox_mode="read-only"`, never `-s`).

session-ended: 2026-06-18 00:00
trigger: typed
memory-review: complete
relational-integration: n/a-no-vault-resident-artifact (the one vault file, Symptom Map.md, was written + already committed by Greg earlier; the vault has a PARALLEL session's uncommitted _work/image-verify-finish-build/ changes that are NOT this session's and were deliberately left untouched per handoff STEP 0a)

## Goal
Get eye-to-eye on what the kl-agents platform IS, via a top-down grill, and build a living SPEC (+ eval criteria) the GSD roadmap rebuilds from. Agile buckets, each Codex-reviewed.

## Touched (this session, kl-agents repo)
- `CONTEXT.md` (NEW, committed 9c1a163 then re-modified): domain glossary — platform spine, two listeners (Scout=market/Echo=owned-audience), the judge (Validation), signal rubric, build sequence, Validation v1 shape, open questions. Re-modified after commit with build-sequence + Validation-shape sections — UNCOMMITTED.
- `SPEC.md` (NEW, committed 9c1a163 then re-modified): Bucket 1 (Scout Re-Aim, Codex-APPROVED) + Bucket 2 (Validation, mid-review). UNCOMMITTED changes = Bucket 2 + the symptom_verbatim fix to Bucket 1.
- `SPEC-EVAL.md` (NEW, committed then re-modified): Bucket 1 + Bucket 2 eval criteria. UNCOMMITTED = Bucket 2 eval.
- `docs/adr/0001-scout-gates-on-signal-rubric-not-belief-match.md` (NEW, committed 9c1a163): the gate re-aim decision.
- `PLAN-REVIEW-LOG.md` (NEW, committed 9c1a163): Bucket 1 Codex transcript (4 rounds to APPROVED). Bucket 2 round 1 NOT yet logged here — needs adding.
- `.planning/` (committed 9c1a163): GSD init — PROJECT.md, REQUIREMENTS.md, ROADMAP.md (PROVISIONAL 3-phase, to be REBUILT from SPEC after grill), config.json, STATE.md.
- Vault `00 K&L Vault/01_Core_Systems/Messaging/Symptom Map.md` (NEW, committed by Greg): the 10-tag symptom vocab, source of truth for Scout's symptom_tag. DO NOT re-commit from this repo's session.

## Open
- **Bucket 2 scope fork (THE resume point):** report+ledger-first vs full-agent. Re-ask Greg, then either trim Bucket 2 or keep as-is, then run Codex round 2 on the revised Bucket 2 (thread 019ed947...). Loop to APPROVED.
- **Bucket 2 round 1 not logged** in PLAN-REVIEW-LOG.md — add it (10 accepted fixes + the 1 open scope question). Fixes applied: symptom_verbatim added to Bucket 1; rank by count not belief-confidence; group by coalesce(theme_other,theme); status/verdict split; can_build_magnet() structural guardrail; no executable threshold defaults (startup error); MIN_THEME/CORPUS_SIGNALS insufficient_data gates; source_signal_id UUID FK; channel CHECK; V0 preflight fail-closed.
- **Grill not finished.** Remaining branches: Bucket 3 (light agent contract, extract against Scout AND Validation not Scout alone), Echo (sequenced last, lightly — built-as-designed). THEN rebuild GSD roadmap from SPEC.
- **Bucket 2 threshold values** (validate/crickets counts) are Greg-set config, deliberately unspecified (honesty rule) — Greg provides real numbers at build time.

## Decisions (do NOT re-litigate)
- **ADR 0001 (signed off):** Scout's keeper-gate flips belief-match → signal rubric (recent + authentic + actionable). Belief/ICA/symptom become nullable DESCRIPTION, not gate. Captures novel off-map signals.
- **Two listeners, distinct:** Scout=market(out), Echo=owned-audience(in, K&L's own LI/FB comments). "Echo sorts it" was loose phrasing — Echo does NOT process Scout output.
- **Signal rubric = authentic operator pain + actionable(restaurant-ops-relevant, NOT playbook-fit) + fresh/recent.** Explicitly NOT belief-fit. G3 must not smuggle belief-fit back in.
- **theme = market-topic axis**, required on kept signals, primary-enum + theme_other for novelty. The connective tissue: Scout emits → trending counts → Validation ranks on it.
- **G2 lean-inclusive** (ambiguous→needs_review, not dropped). Precision (≥80% of 20 kept, Greg-graded) AND recall (20 dropped sampled) measured separately. First round = baseline not gate.
- **Build sequence: Scout re-aim → Validation → Echo.** Complete one value loop first.
- **Platform: LIGHT agent contract**, extracted against Scout+Validation (Validation is NOT a listener).
- **Validation v1:** theme-ranks/verbatim-fills; drafts disposable probes (NOT /kl:write, NOT published content); manual response count v1 (Echo automates later); decision-ledger; crickets/pivot structurally cannot greenlight (can_build_magnet()).
- **GSD: full new-project init chosen** over quick/manual. Roadmap is PROVISIONAL pending SPEC completion.
- **Verifier discipline:** every bucket gets an artifact verifier (mechanical, 0 em-dashes a hard check) + an outcome verifier (real-data) + Codex adversarial review to APPROVED. "Good enough doesn't work."

## Codex
- Bucket 1 thread (APPROVED, done): `019ed158-e3a5-7073-a078-d0c62b22b487`
- Bucket 2 thread (mid-review, round 1 done): `019ed947-0557-76b1-8790-3a78cdda104f`
- Resume rule: `codex exec resume "$THREAD_ID" -c sandbox_mode="read-only" --json -o /tmp/out.txt "..."` — resume REJECTS `-s`. codex-cli 0.130.0 confirmed.
