# Handoff — main — 2026-06-21 00:00

RESUME HERE: Bucket 3 (agent contract) plan is WRITTEN and locked (PLAN.md), but Codex Act 2 review NEVER RAN — Greg interrupted the round-1 launch (review had an issue). Resume = re-run Codex round 1 on PLAN.md, fresh thread, read-only, MAX_ROUNDS=5. The full review prompt is saved at `/tmp/codex_b3_review.txt` (may be gone after reboot — regenerate from the bullet list in this file's Open section if missing). Loop to APPROVED, log to PLAN-REVIEW-LOG.md, then Greg signs off before any code. NOTE: prior Codex threads used `codex exec resume "$ID" -c sandbox_mode="read-only"` (resume REJECTS -s); round 1 is a FRESH session: `codex exec -s read-only --json -o /tmp/out.txt "$(cat prompt)"`. codex-cli 0.130.0.

session-ended: 2026-06-21 00:00
trigger: typed
memory-review: none-this-session
relational-integration: n/a-no-vault-resident-artifact (code-repo session; PLAN.md/SPEC.md live in kl-agents, not the K&L vault relational library)

## Goal
Top-down grill of the kl-agents platform, building a living SPEC (+ eval) the GSD roadmap rebuilds from. Agile buckets, each Codex-reviewed to APPROVED, then Greg sign-off. Bucket order: 1 Scout re-aim (DONE) → 2 Validation (DONE) → 3 agent contract (plan written, review pending) → Echo → roadmap rebuild.

## Touched (this session, kl-agents repo)
- `PLAN.md` (NEW, UNCOMMITTED until this handoff): Bucket 3 plan — light agent contract. Locked decisions + mechanical calls + 6 build slices (3a-3f) + risks + out-of-scope. This is the Act-1 output of grill-with-docs; awaiting Act-2 Codex review.
- (Bucket 2 work from the prior session is already committed at 7219358; nothing from Bucket 2 is open.)

## Open
- **Bucket 3 Codex review (THE resume point):** run Codex round 1 on PLAN.md. Review prompt hunts: (1) does moving StorageConfig to shared/ cut ALL shared→scout imports or are there others; (2) does the JSONB metrics migration 0010 handle live agent_runs history safely + any consumer reading the 4 count columns that breaks when nullable; (3) does generalized finish_run(status, metrics, error_summary) lose info or break Scout's callers (main.py, _finish_safely); (4) is the status/exit-code/partial-failure model coherent for a non-listener (R4); (5) does slice ordering ever leave Scout broken; (6) is the import-direction invariant (shared/ never imports agents/) enforceable + does current code violate it beyond client.py; (7) anything in CONTEXT.md / SPEC.md Bucket 2 the contract fails to support; (8) scope — anything beyond "unblock Validation or fix a real seam" (Q1 rule); is the fetcher-base move the only soft one; (9) simpler alternatives. Read-only every round. End each: VERDICT: APPROVED|REVISE.
- **Bucket 3 plan NOT yet logged in PLAN-REVIEW-LOG.md** — log it as the review runs (like Buckets 1+2).
- **Grill remaining after Bucket 3:** Echo (sequenced last, lightly — built-as-designed). THEN rebuild the PROVISIONAL GSD roadmap (.planning/ROADMAP.md) from the now-3-bucket SPEC.
- **Bucket 2 awaits Greg FINAL sign-off** (Codex-APPROVED 4 rounds, not yet Greg-signed). Spec footer says so. Same gate Bucket 1 passed.
- **last30days skill (side-thread, RESOLVED, no action owed):** Greg asked if mvanhorn/last30days-skill helps Scout. Verdict: install GLOBALLY as a personal research tool (`/plugin marketplace add mvanhorn/last30days-skill` + `brew install yt-dlp`, set `EXCLUDE_SOURCES=tiktok,instagram,threads,pinterest`, skip Perplexity/ScrapeCreators paid sources — redundant with deep-research/firecrawl, off-audience). Do NOT route into Scout or Bucket 3 (it's a one-shot synthesizer, not a corpus-builder; would break Scout's compounding-corpus thesis). Greg had not confirmed install at session end.

## Decisions (do NOT re-litigate)
- **Bucket 3 scope (Q1, Greg-confirmed rule):** Doc + minimal extraction. "Minimal" = a code move is in scope ONLY if it (a) unblocks Validation or (b) fixes a real seam. No refactor-for-elegance, no renames, no speculative abstraction.
- **Decision A (Greg): agent_runs = core columns + JSONB `metrics`.** The 4 listener counts (posts_fetched/dedup_skipped/classified/queued, NOT NULL default 0) are an over-fit; Validation fetches/classifies nothing. Contract owns universal columns; each agent writes its own metrics as JSONB. No fake zeros, no per-agent column sprawl.
- **Decision B (Greg): generalize where free, no speculative code.** Generality is a tiebreaker only; ZERO code for hypothetical agent #4.
- **The seam (Claude, mechanical):** move `StorageConfig` (3 fields, nothing Scout-specific) to `shared/config.py`; both shared/db/client.py and Scout import from shared/. Rejected Protocol (over-machinery) + full-loader-move (scope creep).
- **Grill re-shaped (Claude proposed, Greg agreed):** Bucket 3's branches are mostly internal code-mechanics Greg can't adjudicate. So Claude decides mechanical branches with stated defaults (on the record for Codex), brings Greg ONLY the judgment branches (A + B were the two), Codex stress-tests all of it, Greg signs off the converged plan. This is the right instrument when a bucket is plumbing-heavy.
- **Live-code facts grounding the plan:** shared/db/client.py:16 imports agents.scout.config (the backwards seam). agent_runs (migration 0001) has 4 NOT-NULL-default-0 listener counts. logging_setup.py + fetchers/base.py are already agent-agnostic in design (Scout-located only). storage/posts.py is correctly Scout-specific, STAYS. config.yaml carries agent.name/agent.version top-level for every agent.
- (Carried from prior sessions) ADR 0001 signed; two listeners distinct (Scout=market out / Echo=owned-audience in); Validation is NOT a listener; build sequence Scout→Validation→Echo; full GSD init, roadmap PROVISIONAL pending SPEC; every bucket gets artifact-verifier + outcome-verifier + Codex review; 0 em-dashes is a hard house check.

## Codex
- Bucket 1 thread (APPROVED): `019ed158-e3a5-7073-a078-d0c62b22b487`
- Bucket 2 thread (APPROVED, 4 rounds): `019ed947-0557-76b1-8790-3a78cdda104f`
- Bucket 3: NO thread yet (review never launched). Round 1 is a FRESH `codex exec -s read-only`; capture the thread.started id for resume rounds.
- Resume rule: `codex exec resume "$ID" -c sandbox_mode="read-only" --json -o /tmp/out.txt "..."` — resume REJECTS -s. macOS has no `timeout` cmd; use the Bash tool's own timeout param.
